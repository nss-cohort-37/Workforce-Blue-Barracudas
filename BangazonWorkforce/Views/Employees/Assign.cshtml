@model BangazonWorkforce.Models.ViewModels.AddEmployeeToTPViewModel

@{
    ViewData["Title"] = "Assign";
}

<h1>Assign</h1>

<h4>AddEmployeeToTPViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Assign">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    
            <div class="form-group">
                <label asp-for="TrainingProgramId" class="control-label">Training Programs</label>
            
                <select multiple="multiple" asp-for="TrainingProgramIds" asp-items="@Model.TrainingProgramOptions" class="form-control">
                </select>

            
            </div>
            <div class="form-group">
                <input type="submit" value="Submit" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
